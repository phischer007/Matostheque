# To keep this file secure and not expose it to a version control, meaning not through git or any other

DJANGO_ENV=production  # Set this to 'production' or 'local' as needed
EMAIL_HOST_USER=
EMAIL_HOST=
DB_NAME=matostheque
DB_USER=
DB_PASSWORD=
SECRET_KEY=
